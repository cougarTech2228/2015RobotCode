# 2015RobotCode
TODO:(this week, must absolutely be done, in order that they must be done)
  test motor ramping
  add encoders to testbot
  test speed controls
  test positional controls
  test switching between voltage, positional, and speed
  integrate drive base and elevator/vision code
  autonomous drive code
  add voltage fallback
  use net tables to send pid and ramp values to the bot
  use d-pad on joystick for fine tune positional control
  single conifg file with values for all ports, joystick button mappings, and motor config, etc.
  
